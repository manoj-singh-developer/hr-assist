(() => {

  'use strict';

  angular
    .module('HRA')
    .controller('skillJsonM', skillJsonM);

  skillJsonM
    .$inject = ['$mdDialog', 'skillModel', '$http', '$rootScope'];

  function skillJsonM($mdDialog, skillModel, $http, $rootScope) {

    // TODO: create a example for formJsonCtrl

  }

})();
